import{_ as e,o,c as t,R as a}from"./chunks/framework.EUU6lLMq.js";const b=JSON.parse('{"title":"Installation","description":"","frontmatter":{},"headers":[],"relativePath":"guide/installation.md","filePath":"guide/installation.md"}'),i={name:"guide/installation.md"},s=a(`<h1 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to &quot;Installation&quot;">​</a></h1><h2 id="check-if-your-device-is-supported" tabindex="-1">Check if your device is supported <a class="header-anchor" href="#check-if-your-device-is-supported" aria-label="Permalink to &quot;Check if your device is supported&quot;">​</a></h2><p>Download KernelSU manager APP from <a href="https://github.com/tiann/KernelSU/releases" target="_blank" rel="noreferrer">GitHub Releases</a> and install it to your device:</p><ul><li>If the app shows <code>Unsupported</code>, it means <strong>You should compile the kernel yourself</strong>, KernelSU won&#39;t and never provide a boot image for you to flash.</li><li>If the app shows <code>Not installed</code>, then your devices is officially supported by KernelSU.</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>For devices showing <code>Unsupported</code>, here is an <a href="./unofficially-support-devices.html">Unofficially-support-devices</a>, you can compile the kernel yourself.</p></div><h2 id="backup-stock-boot-img" tabindex="-1">Backup stock boot.img <a class="header-anchor" href="#backup-stock-boot-img" aria-label="Permalink to &quot;Backup stock boot.img&quot;">​</a></h2><p>Before flashing, you must first backup your stock boot.img. If you encounter any bootloop, you can always restore the system by flashing back to the stock factory boot using fastboot.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Flashing may cause data loss, be sure to do this step well before proceeding to the next step!! You can also back up all the data on your phone if necessary.</p></div><h2 id="necessary-knowledge" tabindex="-1">Necessary knowledge <a class="header-anchor" href="#necessary-knowledge" aria-label="Permalink to &quot;Necessary knowledge&quot;">​</a></h2><h3 id="adb-and-fastboot" tabindex="-1">ADB and fastboot <a class="header-anchor" href="#adb-and-fastboot" aria-label="Permalink to &quot;ADB and fastboot&quot;">​</a></h3><p>By default, you will use ADB and fastboot tools in this tutorial, so if you don&#39;t know them, we recommend using a search engine to learn about them first.</p><h3 id="kmi" tabindex="-1">KMI <a class="header-anchor" href="#kmi" aria-label="Permalink to &quot;KMI&quot;">​</a></h3><p>Kernel Module Interface (KMI), kernel versions with the same KMI are <strong>compatible</strong> This is what &quot;general&quot; means in GKI; conversely, if the KMI is different, then these kernels are not compatible with each other, and flashing a kernel image with a different KMI than your device may cause a bootloop.</p><p>Specifically, for GKI devices, the kernel version format should be as follows:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>KernelRelease :=</span></span>
<span class="line"><span>Version.PatchLevel.SubLevel-AndroidRelease-KmiGeneration-suffix</span></span>
<span class="line"><span>w      .x         .y       -zzz           -k            -something</span></span></code></pre></div><p><code>w.x-zzz-k</code> is the KMI version. For example, if a device kernel version is <code>5.10.101-android12-9-g30979850fc20</code>, then its KMI is <code>5.10-android12-9</code>; theoretically, it can boot up normally with other KMI kernels.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Note that the SubLevel in the kernel version is not part of the KMI! That means that <code>5.10.101-android12-9-g30979850fc20</code> has the same KMI as <code>5.10.137-android12-9-g30979850fc20</code>!</p></div><h3 id="security-patch-level" tabindex="-1">Security patch level <a class="header-anchor" href="#security-patch-level" aria-label="Permalink to &quot;Security patch level {#security-patch-level}&quot;">​</a></h3><p>Newer Android devices may have anti-rollback mechanisms in place that do not allow flashing a boot image with an old security patch level. For example, if your device kernel is <code>5.10.101-android12-9-g30979850fc20</code>, its security patch is <code>2023-11</code>; even if you flash the kernel consistent with the kernel KMI, if the security patch level is older than <code>2023- 11</code>(such as <code>2023-06</code>), then it may cause bootloop.</p><p>Therefore, kernels with latest security patch levels are preferred while maintaining KMI consistency.</p><h3 id="kernel-version-vs-android-version" tabindex="-1">Kernel version vs. Android version <a class="header-anchor" href="#kernel-version-vs-android-version" aria-label="Permalink to &quot;Kernel version vs. Android version&quot;">​</a></h3><p>Please note: <strong>Kernel version and Android version are not necessarily the same!</strong></p><p>If you find that your kernel version is <code>android12-5.10.101</code>, but your Android system version is Android 13 or other; please don&#39;t be surprised, because the version number of the Android system is not necessarily the same as the version number of the Linux kernel; The version number of the Linux kernel is generally consistent with the version of the Android system that comes with the <strong>device when it is shipped</strong>. If the Android system is upgraded later, the kernel version will generally not change. If you need to flash, <strong>please always refer to the kernel version!!</strong></p><h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>There are several installation methods for KernelSU, each suitable for a different scenario, so please choose as needed.</p><ol><li>Install with fastboot using the boot.img provided by KernelSU</li><li>Install with a kernel flash app, such as KernelFlasher</li><li>Repair the boot.img manually and install it</li><li>Install with custom Recovery (e.g. TWRP)</li></ol><h2 id="install-with-boot-img-provided-by-kernelsu" tabindex="-1">Install with boot.img provided by KernelSU <a class="header-anchor" href="#install-with-boot-img-provided-by-kernelsu" aria-label="Permalink to &quot;Install with boot.img provided by KernelSU&quot;">​</a></h2><p>If your device&#39;s <code>boot.img</code> uses a commonly used compression format, you can use the GKI images provided by KernelSU to flash it directly. It does not require TWRP or self-patching the image.</p><h3 id="find-proper-boot-img" tabindex="-1">Find proper boot.img <a class="header-anchor" href="#find-proper-boot-img" aria-label="Permalink to &quot;Find proper boot.img&quot;">​</a></h3><p>KernelSU provides a generic boot.img for GKI devices and you should flush the boot.img to the boot partition of the device.</p><p>You can download boot.img from <a href="https://github.com/tiann/KernelSU/releases" target="_blank" rel="noreferrer">GitHub Release</a>, please note that you should use the correct version of boot.img. If you don&#39;t know which file to download, please carefully read the description of <a href="#kmi">KMI</a> and <a href="#security-patch-level">Security Patch Level</a> in this document.</p><p>Normally, there are three boot files in different formats under the same KMI and security patch level. They are all the same except for the kernel compression format. Please check the kernel compression format of your original boot.img. You should use the correct format, such as <code>lz4</code>, <code>gz</code>; if you use an incorrect compression format, you may encounter bootloop after flashing boot.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><ol><li>You can use magiskboot to get the compression format of your original boot; of course you can also ask other, more experienced kids with the same model as your device. Also, the compression format of the kernel usually does not change, so if you boot successfully with a certain compression format, you can try that format later.</li><li>Xiaomi devices usually use <code>gz</code> or <strong>uncompressed</strong>.</li><li>For Pixel devices, follow below instructions.</li></ol></div><h3 id="flash-boot-img-to-device" tabindex="-1">flash boot.img to device <a class="header-anchor" href="#flash-boot-img-to-device" aria-label="Permalink to &quot;flash boot.img to device&quot;">​</a></h3><p>Use <code>adb</code> to connect your device, then execute <code>adb reboot bootloader</code> to enter fastboot mode, then use this command to flash KernelSU:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fastboot</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> flash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> boot</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> boot.img</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If your device supports <code>fastboot boot</code>, you can first use <code>fastboot boot boot.img</code> to try to use boot.img to boot the system first. If something unexpected happens, restart it again to boot.</p></div><h3 id="reboot" tabindex="-1">reboot <a class="header-anchor" href="#reboot" aria-label="Permalink to &quot;reboot&quot;">​</a></h3><p>After flashing is complete, you should reboot your device:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fastboot</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reboot</span></span></code></pre></div><h2 id="install-with-kernel-flasher" tabindex="-1">Install with Kernel Flasher <a class="header-anchor" href="#install-with-kernel-flasher" aria-label="Permalink to &quot;Install with Kernel Flasher&quot;">​</a></h2><p>Step:</p><ol><li>Download the AnyKernel3 zip. If you don&#39;t know which file to download, please carefully read the description of <a href="#kmi">KMI</a> and <a href="#security-patch-level">Security Patch Level</a> in this document.</li><li>Open the Kernel Flash App (grant necessary root permissions) and use the provided AnyKernel3 zip to flash.</li></ol><p>This way requires the kernel flash App to have root permissions. You can use the following methods to achieve this:</p><ol><li><p>Your device has rooted. For example, you have installed KernelSU and want to upgrade to the latest version, or you have rooted through other methods (such as Magisk).</p></li><li><p>If your phone is not rooted, but the phone supports the temporary boot method of <code>fastboot boot boot.img</code>, you can use the GKI image provided by KernelSU to temporarily boot your device, obtain temporary root permissions, and then use the kernel flash Flash the writer to obtain permanent root privileges.</p></li><li><p><a href="https://github.com/capntrips/KernelFlasher/releases" target="_blank" rel="noreferrer">Kernel Flasher</a></p></li><li><p><a href="https://play.google.com/store/apps/details?id=com.franco.kernel" target="_blank" rel="noreferrer">Franco Kernel Manager</a></p></li><li><p><a href="https://play.google.com/store/apps/details?id=flar2.exkernelmanager" target="_blank" rel="noreferrer">Ex Kernel Manager</a></p></li></ol><p>PS. This method is more convenient when upgrading KernelSU and can be done without a computer (backup first!). .</p><h2 id="patch-boot-img-manually" tabindex="-1">Patch boot.img manually <a class="header-anchor" href="#patch-boot-img-manually" aria-label="Permalink to &quot;Patch boot.img manually&quot;">​</a></h2><p>For some devices, the boot.img format is not so common, such as not <code>lz4</code>, <code>gz</code> and uncompressed; the most typical is Pixel, its boot.img format is <code>lz4_legacy</code> compressed, ramdisk may be <code>gz</code> may also be <code>lz4_legacy</code> compression; at this time, if you directly flash the boot.img provided by KernelSU, the phone may not be able to boot; at this time, you can manually patch the boot.img to achieve.</p><p>It&#39;s always recommended to use <code>magiskboot</code> to patch images, there are two ways:</p><ol><li><a href="https://github.com/topjohnwu/Magisk/releases" target="_blank" rel="noreferrer">magiskboot</a></li><li><a href="https://github.com/ookiineko/magiskboot_build/releases/tag/last-ci" target="_blank" rel="noreferrer">magiskboot_build</a></li></ol><p>The official build of <code>magiskboot</code> can only run on Android devices, if you want to run it on PC, you can try the second one.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Android-Image-Kitchen is not recommended now, because it doesn&#39;t handle the boot metadata(such as security patch level) correctly, thus it may not work on some devices.</p></div><h3 id="preparation" tabindex="-1">Preparation <a class="header-anchor" href="#preparation" aria-label="Permalink to &quot;Preparation&quot;">​</a></h3><ol><li>Get your phone&#39;s stock boot.img; you can get it from your device manufacturers, you may need <a href="https://github.com/ssut/payload-dumper-go" target="_blank" rel="noreferrer">payload-dumper-go</a></li><li>Download the AnyKernel3 zip file provided by KernelSU that matches the KMI version of your device (you can refer to the <em>Install with custom Recovery</em>).</li><li>Unpack the AnyKernel3 package and get the <code>Image</code> file, which is the kernel file of KernelSU.</li></ol><h3 id="using-magiskboot-on-Android-devices" tabindex="-1">Using magiskboot on Android devices <a class="header-anchor" href="#using-magiskboot-on-Android-devices" aria-label="Permalink to &quot;Using magiskboot on Android devices {#using-magiskboot-on-Android-devices}&quot;">​</a></h3><ol><li>Download latest Magisk from <a href="https://github.com/topjohnwu/Magisk/releases" target="_blank" rel="noreferrer">Release Page</a></li><li>Rename <code>Magisk-*(version).apk</code> to <code>Magisk-*.zip</code> and unzip it.</li><li>Push <code>Magisk-*/lib/arm64-v8a/libmagiskboot.so</code> to your device by adb: <code>adb push Magisk-*/lib/arm64-v8a/libmagiskboot.so /data/local/tmp/magiskboot</code></li><li>Push stock boot.img and Image in AnyKernel3 to your device.</li><li>Enter adb shell and cd <code>/data/local/tmp/</code> directory, then <code>chmod +x magiskboot</code></li><li>Enter adb shell and cd <code>/data/local/tmp/</code> directory, execute <code>./magiskboot unpack boot.img</code> to unpack <code>boot.img</code>, you will get a <code>kernel</code> file, this is your stock kernel.</li><li>Replace <code>kernel</code> with <code>Image</code>: <code>mv -f Image kernel</code></li><li>Execute <code>./magiskboot repack boot.img</code> to repack boot img, and you will get a <code>new-boot.img</code> file, flash this file to device by fastboot.</li></ol><h3 id="using-magiskboot-on-PC" tabindex="-1">Using magiskboot on Windows/macOS/Linux PC <a class="header-anchor" href="#using-magiskboot-on-PC" aria-label="Permalink to &quot;Using magiskboot on Windows/macOS/Linux PC{#using-magiskboot-on-PC}&quot;">​</a></h3><ol><li>Download proper <code>magiskboot</code> for your OS from <a href="https://github.com/ookiineko/magiskboot_build/releases/tag/last-ci" target="_blank" rel="noreferrer">magiskboot_build</a></li><li>Prepare stock boot.img and Image in your PC.</li><li><code>chmod +x magiskboot</code></li><li>Enter the proper directory, execute <code>./magiskboot unpack boot.img</code> to unpack <code>boot.img</code>, you will get a <code>kernel</code> file, this is your stock kernel.</li><li>Replace <code>kernel</code> with <code>Image</code>: <code>mv -f Image kernel</code></li><li>Execute <code>./magiskboot repack boot.img</code> to repack boot img, and you will get a <code>new-boot.img</code> file, flash this file to device by fastboot.</li></ol><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Official <code>magiskboot</code> can run <code>Linux</code> device normally, if you are a Linux user, you can use official build.</p></div><h2 id="install-with-custom-recovery" tabindex="-1">Install with custom Recovery <a class="header-anchor" href="#install-with-custom-recovery" aria-label="Permalink to &quot;Install with custom Recovery&quot;">​</a></h2><p>Prerequisite: Your device must have a custom Recovery, such as TWRP; if not or only official Recovery is available, use another method.</p><p>Step:</p><ol><li>From the <a href="https://github.com/tiann/KernelSU/releases" target="_blank" rel="noreferrer">Release page</a> of KernelSU, download the zip package starting with AnyKernel3 that matches your phone version; for example, the phone kernel version is <code>android12-5.10. 66</code>, then you should download the file <code>AnyKernel3-android12-5.10.66_yyyy-MM.zip</code> (where <code>yyyy</code> is the year and <code>MM</code> is the month).</li><li>Reboot the phone into TWRP.</li><li>Use adb to put AnyKernel3-*.zip into the phone /sdcard and choose to install it in the TWRP GUI; or you can directly <code>adb sideload AnyKernel-*.zip</code> to install.</li></ol><p>PS. This method is suitable for any installation (not limited to initial installation or subsequent upgrades), as long as you use TWRP.</p><h2 id="other-methods" tabindex="-1">Other methods <a class="header-anchor" href="#other-methods" aria-label="Permalink to &quot;Other methods&quot;">​</a></h2><p>In fact, all these installation methods have only one main idea, which is to <strong>replace the original kernel for the one provided by KernelSU</strong>; as long as this can be achieved, it can be installed; for example, the following are other possible methods.</p><ol><li>First install Magisk, get root privileges through Magisk and then use the kernel flasher to flash in the AnyKernel zip from KernelSU.</li><li>Use some flashing toolkit on PCs to flash in the kernel provided KernelSU.</li></ol><p>But if it doesn&#39;t work, please try <code>magiskboot</code> way.</p>`,68),l=[s];function r(n,c,d,h,p,u){return o(),t("div",null,l)}const g=e(i,[["render",r]]);export{b as __pageData,g as default};
